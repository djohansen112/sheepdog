#Thes can be flat, with a single gate. Player starts in the middle, 
#with the flock all around scattere randomly. Once the sheep go through 
#the gate they should bunch up until they are all put away. Higher levels 
#can have obtacles like trees or other gates to get through.

extends Node2D


# Called when the node enters the scene tree for the first time.
func _ready():
	pass # Replace with function body.


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta):
	pass
